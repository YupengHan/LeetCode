class Solution {
public:
    int oddEvenJumps(vector<int>& arr) {
        int n = arr.size();
        vector<bool> odd_list (n, false);
        vector<bool> eve_list (n, false);
        odd_list[n-1] = true;
        eve_list[n-1] = true;


        
    }
};