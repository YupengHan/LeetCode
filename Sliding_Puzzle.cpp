class Solution {
    vector<bool> expand_map;
    vector<vector<int>> move;
    int ret = 0;
    vector<int> gt1(1,2,3);
    vector<int> gt2(4,5,0);
public:
    int slidingPuzzle(vector<vector<int>>& board) {    
        vector<>
        expand_map[]
        if (board[0] == gt1 && board[1] == gt2) {
            return ret;
        }
        else 
    }
    
};