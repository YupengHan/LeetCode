class Solution {
public:
    bool exist(vector<vector<char>>& board, string word) {
        if (word.size() == 0) {
            return true;
        }
        bool ret = false;

        for (int i = 0; i < borad.size(); i++) {
            for (int j = 0; j < borad[0].size(); j++) {
                char cur_char = word[0];
                if (board[i][j] == ) {
                    
                }
                
            }
        }
        
    }
    bool find_rest_word(vector<vector<char>>& board, string word, int i, int j) {

    }
};